server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/founderlink_messages?createDatabaseIfNotExist=true
    username: root
    password: MySql@2026

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
