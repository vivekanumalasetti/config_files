server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/founderlink_teams?createDatabaseIfNotExist=true
    username: root
    password: MySql@2026

  jpa:
    hibernate:
      ddl-auto: update

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

rabbitmq:
  exchange: founderlink.exchange

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
