server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/founderlink_notifications?createDatabaseIfNotExist=true
    username: root
    password: MySql@2026

  jpa:
    hibernate:
      ddl-auto: update

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

rabbitmq:
  queue:
    startup: startup.queue
    investment: investment.queue
    team: team.queue

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
